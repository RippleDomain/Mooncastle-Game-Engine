<UserControl
    x:Class="MooncastleEditor.Editors.GameEntityView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:MooncastleEditor.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MooncastleEditor.Editors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mooncastle="clr-namespace:MooncastleEditor"
    xmlns:vm="clr-namespace:MooncastleEditor.Components"
    d:DataContext="{d:DesignInstance Type=vm:MSGameEntity}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="{DynamicResource Editor.Window.GrayBrush3}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <mooncastle:NullableBooltoBoolConverter x:Key="nullableBooltoBoolConverter" />
        <DataTemplate DataType="{x:Type vm:MSTransform}">
            <local:TransformComponentView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MSScript}">
            <local:ScriptComponentView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MSGeometry}">
            <local:GeometryComponentView />
        </DataTemplate>
        <ContextMenu x:Key="addComponentMenu" IsOpen="{Binding Path=IsChecked, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}">
            <MenuItem Click="OnAddGeometryComponent" Header="Geometry" />
            <MenuItem Header="Physics">
                <MenuItem Header="Rigidbody" />
            </MenuItem>
            <MenuItem Header="Script" ItemsSource="{Binding SelectedEntities[0].ParentScene.Project.AvailableScripts}">
                <MenuItem.Style>
                    <Style TargetType="{x:Type MenuItem}">
                        <Setter Property="Padding" Value="5,3,2,3" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedEntities[0].ParentScene.Project.AvailableScripts}" Value="{x:Null}">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </MenuItem.Style>
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="{x:Type MenuItem}">
                        <Setter Property="Padding" Value="5,3,2,3" />
                        <Setter Property="Header" Value="{Binding}" />
                        <EventSetter Event="Click" Handler="OnAddScriptComponent" />
                    </Style>
                </MenuItem.ItemContainerStyle>
            </MenuItem>
        </ContextMenu>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <ScrollViewer.Style>
            <Style TargetType="ScrollViewer">
                <Style.Triggers>
                    <Trigger Property="DataContext" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ScrollViewer.Style>
        <StackPanel VerticalAlignment="Top" Background="{DynamicResource Editor.Window.GrayBrush4}">
            <Border Height="32" Background="{DynamicResource Editor.Window.GrayBrush3}">
                <ToggleButton
                    x:Name="addComponent"
                    Margin="5,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    ClickMode="Press"
                    Focusable="False"
                    IsEnabled="{Binding IsEnabled, Converter={StaticResource nullableBooltoBoolConverter}}"
                    PreviewMouseLeftButtonDown="OnAddComponent_Button_PreviewMouse_LBD">
                    <ToggleButton.Content>
                        <DockPanel>
                            <TextBlock Margin="5,0" Text="Add Component" />
                            <Path
                                Margin="3,0,2,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="M0, 0 L0, 2 L4, 6 L8, 2 L8, 0 L4, 4 z"
                                Fill="{DynamicResource Editor.FontBrush}" />
                        </DockPanel>
                    </ToggleButton.Content>
                </ToggleButton>
            </Border>
            <Grid
                Height="32"
                Margin="0,1"
                Background="{DynamicResource Editor.Window.GrayBrush3}"
                SnapsToDevicePixels="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="78" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Margin="5,0,0,0"
                    IsEnabled="{Binding IsEnabled, Converter={StaticResource nullableBooltoBoolConverter}}"
                    Style="{StaticResource LightTextBlockStyle}"
                    Text="Name" />
                <TextBox
                    Grid.Column="1"
                    GotKeyboardFocus="OnName_TextBox_GotKeyboardFocus"
                    IsEnabled="{Binding IsEnabled, Converter={StaticResource nullableBooltoBoolConverter}}"
                    LostKeyboardFocus="OnName_TextBox_LostKeyboardFocus"
                    Text="{Binding Name, UpdateSourceTrigger=Explicit}" />
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock
                        Margin="5,0,0,0"
                        Style="{StaticResource LightTextBlockStyle}"
                        Text="Enabled" />
                    <CheckBox
                        Margin="5,0"
                        VerticalAlignment="Center"
                        Click="OnIsEnabled_CheckBox_Click"
                        IsChecked="{Binding IsEnabled, Mode=OneWay}" />
                </StackPanel>
            </Grid>
            <ItemsControl
                IsEnabled="{Binding IsEnabled, Converter={StaticResource nullableBooltoBoolConverter}}"
                IsTabStop="False"
                ItemsSource="{Binding Components}"
                SnapsToDevicePixels="True">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
