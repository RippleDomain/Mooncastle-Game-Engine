<UserControl
    x:Class="MooncastleEditor.Editors.TransformComponentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MooncastleEditor.Editors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utl="clr-namespace:MooncastleEditor.Utilities.Controls"
    xmlns:vm="clr-namespace:MooncastleEditor.Components"
    d:DataContext="{d:DesignInstance Type=vm:MSTransform}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource LightTextBlockStyle}" TargetType="{x:Type TextBlock}" />
    </UserControl.Resources>
    <utl:ComponentView Header="Transform">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="78" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Text="Position" />
            <utl:VectorBox
                Grid.Row="0"
                Grid.Column="1"
                LostKeyboardFocus="OnPosition_VectorBox_LostKeyboardFocus"
                PreviewMouseLeftButtonDown="OnPosition_VectorBox_PreviewMouse_LBD"
                PreviewMouseLeftButtonUp="OnPosition_VectorBox_PreviewMouse_LBU"
                X="{Binding PosX}"
                Y="{Binding PosY}"
                Z="{Binding PosZ}" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Text="Rotation" />
            <utl:VectorBox
                Grid.Row="1"
                Grid.Column="1"
                LostKeyboardFocus="OnRotation_VectorBox_LostKeyboardFocus"
                PreviewMouseLeftButtonDown="OnRotation_VectorBox_PreviewMouse_LBD"
                PreviewMouseLeftButtonUp="OnRotation_VectorBox_PreviewMouse_LBU"
                X="{Binding RotX}"
                Y="{Binding RotY}"
                Z="{Binding RotZ}" />
            <TextBlock
                Grid.Row="2"
                Grid.Column="0"
                Text="Scale" />
            <utl:VectorBox
                Grid.Row="2"
                Grid.Column="1"
                LostKeyboardFocus="OnScale_VectorBox_LostKeyboardFocus"
                PreviewMouseLeftButtonDown="OnScale_VectorBox_PreviewMouse_LBD"
                PreviewMouseLeftButtonUp="OnScale_VectorBox_PreviewMouse_LBU"
                X="{Binding ScaleX}"
                Y="{Binding ScaleY}"
                Z="{Binding ScaleZ}" />
        </Grid>
    </utl:ComponentView>
</UserControl>
