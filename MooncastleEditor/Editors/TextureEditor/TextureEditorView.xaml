<UserControl
    x:Class="MooncastleEditor.Editors.TextureEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:content="clr-namespace:MooncastleEditor.Content"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ed="clr-namespace:MooncastleEditor.Editors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    FocusVisualStyle="{x:Null}"
    Focusable="True"
    mc:Ignorable="d">
    <UserControl.CommandBindings>
        <CommandBinding Command="{x:Static ed:TextureViewCommands.CenterCommand}" Executed="OnCenterTexture" />
        <CommandBinding Command="{x:Static ed:TextureViewCommands.ZoomInCommand}" Executed="OnZoomInTexture" />
        <CommandBinding Command="{x:Static ed:TextureViewCommands.ZoomOutCommand}" Executed="OnZoomOutTexture" />
        <CommandBinding Command="{x:Static ed:TextureViewCommands.ZoomFitCommand}" Executed="OnZoomFitTexture" />
        <CommandBinding Command="{x:Static ed:TextureViewCommands.ActualSizeCommand}" Executed="OnActualSizeTexture" />
    </UserControl.CommandBindings>
    <UserControl.Resources>
        <Style BasedOn="{StaticResource LightTextBlockStyle}" TargetType="{x:Type TextBlock}" />
    </UserControl.Resources>
    <UserControl.DataContext>
        <ed:TextureEditor />
    </UserControl.DataContext>
    <DockPanel d:IsEnabled="True" Background="{DynamicResource Editor.Window.GrayBrush2}">
        <DockPanel.Style>
            <Style TargetType="{x:Type DockPanel}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Texture}" Value="{x:Null}">
                        <Setter Property="IsEnabled" Value="False" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DockPanel.Style>
        <Border
            Height="32"
            Margin="0,0,0,1"
            VerticalAlignment="Top"
            Background="{DynamicResource Editor.Window.GrayBrush3}"
            DockPanel.Dock="Top">
            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="10"
                    Opacity="0.3"
                    ShadowDepth="0" />
            </Border.Effect>
            <StackPanel Margin="5,2" Orientation="Horizontal">
                <Button
                    Margin="0,0,2,0"
                    VerticalAlignment="Stretch"
                    Command="{Binding SetAllChannelsCommand}"
                    Content="All" />
                <ToggleButton
                    Margin="0,0,2,0"
                    VerticalAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Command="{Binding SetChannelCommand}"
                    CommandParameter="0"
                    IsChecked="{Binding IsRedChannelSelected, Mode=OneWay}">
                    <Rectangle
                        Width="8"
                        Margin="0,3,0,0"
                        Fill="{DynamicResource Editor.RedBrush}"
                        IsHitTestVisible="False" />
                </ToggleButton>
                <ToggleButton
                    Margin="0,0,2,0"
                    VerticalAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Command="{Binding SetChannelCommand}"
                    CommandParameter="1"
                    IsChecked="{Binding IsGreenChannelSelected, Mode=OneWay}">
                    <Rectangle
                        Width="8"
                        Margin="0,3,0,0"
                        Fill="{DynamicResource Editor.GreenBrush}"
                        IsHitTestVisible="False" />
                </ToggleButton>
                <ToggleButton
                    Margin="0,0,2,0"
                    VerticalAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Command="{Binding SetChannelCommand}"
                    CommandParameter="2"
                    IsChecked="{Binding IsBlueChannelSelected, Mode=OneWay}">
                    <Rectangle
                        Width="8"
                        Margin="0,3,0,0"
                        Fill="{DynamicResource Editor.BlueBrush}"
                        IsHitTestVisible="False" />
                </ToggleButton>
                <ToggleButton
                    Margin="0,0,2,0"
                    VerticalAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Command="{Binding SetChannelCommand}"
                    CommandParameter="3"
                    IsChecked="{Binding IsAlphaChannelSelected, Mode=OneWay}">
                    <Rectangle
                        Width="8"
                        Margin="0,3,0,0"
                        Fill="{DynamicResource Editor.FontBrush}"
                        IsHitTestVisible="False" />
                </ToggleButton>
                <ToggleButton
                    Margin="5,0"
                    Padding="5"
                    VerticalAlignment="Stretch"
                    d:Visibility="Visible"
                    Command="{Binding RegenerateBitmapsCommand}"
                    CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                    Content="View as Normal Map"
                    IsChecked="{Binding Texture.IsNormalMap, Mode=OneWay}">
                    <ToggleButton.Style>
                        <Style BasedOn="{StaticResource MooncastleButtonStyle}" TargetType="{x:Type ToggleButton}">
                            <Setter Property="IsEnabled" Value="False" />
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Texture.Format}" Value="{x:Static content:DXGI_FORMAT.DXGI_FORMAT_BC5_UNORM}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>
                <TextBlock
                    Margin="5,0"
                    Text="Layer:"
                    Visibility="{Binding Path=Visibility, ElementName=arraySlider}" />
                <Slider
                    x:Name="arraySlider"
                    Width="200"
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    d:Visibility="Visible"
                    Interval="1"
                    IsSnapToTickEnabled="True"
                    Maximum="{Binding MaxArrayIndex}"
                    Minimum="0"
                    TickPlacement="BottomRight"
                    Value="{Binding ArrayIndex}">
                    <Slider.Style>
                        <Style TargetType="{x:Type Slider}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Texture.IsVolumeMap}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding MaxArrayIndex}" Value="0">
                                    <Setter Property="IsEnabled" Value="False" />
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Slider.Style>
                </Slider>
                <TextBlock
                    Margin="5,0"
                    Text="Depth:"
                    Visibility="{Binding Path=Visibility, ElementName=depthSlider}" />
                <Slider
                    x:Name="depthSlider"
                    Width="200"
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    d:Visibility="Visible"
                    Interval="1"
                    IsSnapToTickEnabled="True"
                    Maximum="{Binding MaxDepthIndex}"
                    Minimum="0"
                    TickPlacement="BottomRight"
                    Value="{Binding DepthIndex}">
                    <Slider.Style>
                        <Style TargetType="{x:Type Slider}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Texture.IsVolumeMap}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding MaxDepthIndex}" Value="0">
                                    <Setter Property="IsEnabled" Value="False" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Slider.Style>
                </Slider>
                <TextBlock
                    Margin="5,0"
                    Text="Mip:"
                    Visibility="{Binding Path=Visibility, ElementName=mipSlider}" />
                <Slider
                    x:Name="mipSlider"
                    Width="200"
                    VerticalAlignment="Center"
                    d:Visibility="Visible"
                    Interval="1"
                    IsMoveToPointEnabled="True"
                    IsSnapToTickEnabled="True"
                    Maximum="{Binding MaxMipIndex}"
                    Minimum="0"
                    TickPlacement="BottomRight"
                    Value="{Binding MipIndex}">
                    <Slider.Style>
                        <Style TargetType="{x:Type Slider}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding MaxMipIndex}" Value="0">
                                    <Setter Property="IsEnabled" Value="False" />
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Slider.Style>
                </Slider>

            </StackPanel>
        </Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="250" MaxWidth="400" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ed:TextureDetailsView Margin="0,0,5,0" />
            <GridSplitter
                Width="5"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                Background="{DynamicResource Editor.Window.GrayBrush1}" />
            <ed:TextureView x:Name="textureView" Grid.Column="1" />
        </Grid>
    </DockPanel>
</UserControl>
